# 修复说明 - 安装包问题

## 问题原因
安装时提示"无效安装包"和"与操作系统不兼容"是因为：

1. **包名格式问题**：`applicationId = "com.zhenci.app.v2"` 包含 `.v2` 后缀，某些Android版本可能不兼容这种格式
2. **版本冲突**：如果之前安装过旧版本，包名变化可能导致冲突

## 修复内容

已修改 `app/build.gradle.kts`：

```kotlin
// 修改前
applicationId = "com.zhenci.app.v2"
versionCode = 2
versionName = "1.0.1"

// 修改后  
applicationId = "com.zhenci.app"
versionCode = 3
versionName = "1.0.2"
```

## 提交信息

```bash
git add app/build.gradle.kts
git commit -m "fix: 修复包名格式和版本号

- 将 applicationId 从 com.zhenci.app.v2 改为 com.zhenci.app
- 版本号升级到 1.0.2 (versionCode 3)

修复安装时提示'无效安装包'和'与操作系统不兼容'的问题"
```

## 推送并构建

由于当前网络无法连接到GitHub，需要手动推送：

```bash
cd /Users/king/Desktop/针刺-app
git push origin main
```

推送后会自动触发GitHub Actions构建，构建完成后可以从以下地址下载APK：
https://github.com/NickWang2023/zhenci-app/actions

## 本地构建（备用方案）

如果GitHub Actions不可用，可以在本地构建：

1. 安装 Android Studio
2. 打开项目 `/Users/king/Desktop/针刺-app`
3. Build → Generate Signed Bundle/APK → APK
4. 选择 Release 或 Debug 构建

或者使用命令行（需要配置Java环境）：
```bash
cd /Users/king/Desktop/针刺-app
./gradlew assembleRelease
```

APK将生成在：`app/build/outputs/apk/release/`
